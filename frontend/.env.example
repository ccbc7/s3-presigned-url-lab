# デプロイ後、CloudFormation の Outputs(ApiBaseUrl) の値を設定する。
# このファイルを .env にコピーして使う:  cp .env.example .env
VITE_API_BASE_URL=https://xxxxxxxxxx.execute-api.ap-northeast-1.amazonaws.com

# 補足: 表示用の CloudFront URL は API レスポンス(cdnUrl)に含まれるため、
# フロント側で CDN ドメインを設定する必要はありません。
